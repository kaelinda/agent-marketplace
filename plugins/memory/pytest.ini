[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers --tb=short
markers =
    contract: shared adapter contract test (parametrised over backends)
    network: requires network access to a real backend (skipped by default)
