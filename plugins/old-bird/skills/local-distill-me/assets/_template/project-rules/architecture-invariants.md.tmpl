# 架构红线 / 不可违背约束 — {{PROJECT}}

> 本项目稳定的硬约束：违背 = 事故。**改架构 / 重构 / 抽公共逻辑前必读**。按需读取。
> 可选规则——不需要就删本文件 + 删 `CLAUDE.local.md` 里对应条目。

## 约束清单

每条注明：**约束 + 为什么 + 违背的后果**。

<!-- 示例（删掉换成真实约束）：
- <模块 X> 永久独立，不与 <Y> 共享代码 / 不互相 import —— 因为 <原因>；违背会 <后果>
- 不要绕过 <某层 / 某机制>（如 IPC / 某 service）直接调 <底层> —— 因为 <原因>
- <某类代码> 只能放在 <某目录> —— 因为 <原因>
-->

（初始为空，把项目里"不能这么做"的硬规则沉淀进来。）
