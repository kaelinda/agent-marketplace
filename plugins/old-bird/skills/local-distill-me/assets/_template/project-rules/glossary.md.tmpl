# 术语 / 缩写表 — {{PROJECT}}

> 项目黑话、缩写、状态码语义——防止 Claude 误读领域词。按需读取。
> 可选规则——不需要就删本文件 + 删 `CLAUDE.local.md` 里对应条目。

## 术语

<!-- 示例（删掉换成真实术语）：
- `<缩写>` = <全称 / 含义>
- 状态码 <N> = <语义>
- <内部黑话> = <白话解释>
-->

（初始为空，遇到一个会被误读的领域词就记一条。）
