# 日志 / 埋点约定 — {{PROJECT}}

> 如果本项目有统一的日志 / 埋点 / 监控命名规范，写在这里。没有就删掉本文件，并把
> `CLAUDE.local.md` 里对应的「按需读取」条目一并删掉。

## 命名约定

- <填入：事件名 / step 命名规则，例如「中文事件名、不带前缀、不带编号」>

## 反模式（不要这么写）

- <填入：常见错误写法>

## 改造检查清单

```bash
# <填入：改完日志后用来反扫的 grep 命令>
```
