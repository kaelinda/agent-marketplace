# 坑点 / 反模式 — {{PROJECT}}

> 记录代码里看不出来的隐性知识：踩过的坑、易错的反模式。**按需读取**（刻意不进强制加载，避免列表增长后吃掉每会话上下文）。
> 可选规则——不需要就删本文件 + 删 `CLAUDE.local.md` 里对应条目。

## 记录约定

- **格式**：一条一段，`日期 | 坑（现象）| 根因 | 规避规则`
- **何时记**：工作中踩到 / 发现非显然的坑 → 主动追加；或用户说「记一下这个坑」
- **收敛治理**（防膨胀）：只留高频高危的 Top-N；坑被代码修复 / 过时 → 标「已过时」或删除；列表过长时把细节沉到记忆系统（如有），本表只保留精华
- **要不要每会话强制？** 默认按需即可。若某条坑必须每会话都提醒 Claude，把它单独拎一句放进 `CLAUDE.local.md` 强制加载组，**不要**把整张表设成强制

## 坑点清单

<!-- 示例（删掉换成真实条目）：
- 2026-01-01 | 调 X 时偶发空指针 | Y 异步未 await | 调 X 前必须先 await Z
-->

（初始为空，踩到一个记一个。）
