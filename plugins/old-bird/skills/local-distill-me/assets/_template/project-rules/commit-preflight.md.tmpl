## 提交前红线（COMMIT PRE-FLIGHT）— {{PROJECT}}

> 本文件登记「日常本地调试时常打开、但 commit / push / 提测 / 发版前必须回滚」的开关与改动。
> Claude 作为最后一道闸门：每次 commit/push 前，无论用户是否提醒，必须按下表主动扫描并确认回滚。
> 项目初始化时此表为空——把本项目实际的测试沙盒开关 / mock 开关 / 本地拦截开关补进来。

### 测试 / 调试开关清单

| 文件 | 变量 / 标记 | 干净提交态 | 打开态意味着 |
|---|---|---|---|
| `<填入文件路径>` | `<填入变量名>` | `<期望值>` | `<打开后会发生什么>` |

> 出现新的沙盒入口 / mock 开关时，把行加进上表。

### Claude 行为约束（每次 commit / push 必检）

1. **写代码前**：用户要求"固定测试 XXX"可以打开对应开关，但必须同步告知"提交前会自动检查恢复"。
2. **commit / push 之前**：用 `grep` 扫描上表所有标记，凡处于测试态的必须停下来跟用户确认是否回滚。
3. **不要靠用户记忆**：用户可能忘了开关还开着；Claude 必须主动扫描。
4. **生成 commit message 时**：若刚改过任何测试开关，把回滚作为同一次 commit 的一部分，不要带着测试态提交。

### lint / typecheck（如项目有）

- 提交前对改动文件跑 lint / typecheck，0 error 才提交（与上面的开关扫描一并执行）。

### 分批 commit 保留测试态（如项目这么约定）

- 分批提交时，测试态文件留工作区不入 staging：每批严格 `git add <具体文件路径>`，禁用 `git add -A` / `.` / `-u`，禁用 stash / checkout 回滚。
- 每批 `git add` 后 `git diff --cached --stat` 核对没有测试态文件混入。
